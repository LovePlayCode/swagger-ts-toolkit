# 🚀 发布清单

## 发布前检查

### 📋 代码质量
- [ ] 所有测试通过 (`npm test`)
- [ ] 代码通过 linting (`npm run lint`)
- [ ] 代码格式正确 (`npm run format`)
- [ ] 构建成功 (`npm run build`)
- [ ] TypeScript 编译无错误

### 📚 文档
- [ ] README.md 更新完整
- [ ] CHANGELOG.md 更新（如果有）
- [ ] API 文档同步
- [ ] 示例代码测试通过

### 🔧 配置文件
- [ ] package.json 版本号正确
- [ ] package.json 信息完整（描述、关键词、作者等）
- [ ] LICENSE 文件存在
- [ ] .npmignore 或 package.json files 字段配置正确

### 🧪 功能测试
- [ ] CLI 工具正常工作
- [ ] 完整命令测试：`npx swagger-ts-toolkit --help`
- [ ] 简短命令测试：`npx stt --help`
- [ ] 初始化命令测试：`npx stt init`
- [ ] 生成命令测试（如果有测试用例）

## 发布步骤

### 1. 准备发布
```bash
# 确保在 main 分支
git checkout main

# 拉取最新代码
git pull origin main

# 安装依赖
npm ci

# 运行完整测试
npm test

# 构建项目
npm run build
```

### 2. 版本管理
```bash
# 更新版本号（选择一个）
npm version patch  # 1.0.0 -> 1.0.1 (bug fixes)
npm version minor  # 1.0.0 -> 1.1.0 (new features)
npm version major  # 1.0.0 -> 2.0.0 (breaking changes)
```

### 3. 发布到 npm
```bash
# 干运行检查
npm publish --dry-run

# 正式发布
npm publish
```

### 4. 推送标签
```bash
# 推送版本标签
git push origin main --tags
```

### 5. GitHub Release
- [ ] 在 GitHub 创建 Release
- [ ] 添加版本说明
- [ ] 上传构建产物（如果需要）

## 发布后检查

### 📦 NPM 包
- [ ] 包在 npm registry 可见
- [ ] 安装测试：`npm install swagger-ts-toolkit`
- [ ] 全局安装测试：`npm install -g swagger-ts-toolkit`

### 🔗 链接检查
- [ ] npm 包页面信息正确
- [ ] GitHub 链接正常
- [ ] 文档链接正常

### 📢 通知
- [ ] 更新项目 README badges（如果有）
- [ ] 社区通知（如果适用）
- [ ] 更新相关项目依赖

## 回滚计划

如果发布出现问题：

```bash
# 撤销 npm 发布（24小时内）
npm unpublish swagger-ts-toolkit@<version>

# 或者发布修复版本
npm version patch
npm publish
```

## 版本规范

遵循 [Semantic Versioning](https://semver.org/)：

- **MAJOR**: 不兼容的 API 更改
- **MINOR**: 向后兼容的功能添加  
- **PATCH**: 向后兼容的错误修复

## 注意事项

- 🚨 发布前务必测试 CLI 功能
- 🚨 确保所有示例代码可运行
- 🚨 检查 peerDependencies 兼容性
- 🚨 验证生成的类型文件格式正确