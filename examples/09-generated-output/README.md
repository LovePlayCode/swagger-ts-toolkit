# 📤 生成文件示例

本目录展示 swagger-ts-toolkit 生成的典型输出文件。

## 📂 文件说明

- **api-functions-example.ts** - 生成的 API 函数示例（带类型定义）
- **usage-example.ts** - 生成代码的使用示例

## 🎯 典型生成文件

生成的代码包括：

1. **API 类型定义** (`.d.ts`)
   - 请求/响应类型
   - 错误类型定义
   - 枚举和常量

2. **API 函数** (`.ts`)
   - 类型安全的 API 调用函数
   - 自动参数验证
   - 响应类型推断

3. **端点定义** (`.ts`)
   - API 端点常量
   - 路由信息
   - 请求方法

## 💡 使用生成的文件

```typescript
import { getUserById, updateUser } from './generated/api-functions';
import type { User, UpdateUserRequest } from './generated/api-types';

// 完全的类型推断和检查
const user: User = await getUserById({ userId: '123' });
const updated = await updateUser({ userId: '123', body: {...} });
```

## 📚 自定义生成

配置生成选项：
- 输出目录
- 文件命名规则
- 导出格式
- 类型生成方式

详见 `../02-configuration/` 了解更多配置选项。
